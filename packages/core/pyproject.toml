[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "judgewrite-core"
version = "0.1.0"
description = "Shared core services for JudgeWrite"
requires-python = ">=3.9"
dependencies = [
  "pydantic>=2,<3",
  "requests>=2.31,<3"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
